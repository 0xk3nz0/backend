filebeat.inputs:
- type: log
  enabled: true
  paths:
    - /app/logs/*.log

output.console:
  enabled: true

# Enable Logstash output
output.logstash:
  enabled: true
  hosts: ["logstash:5044"]
# logstash is the container name